- name: 自动提交更新后的m3u文件
  run: |
    git config --global user.name "GitHub Actions"
    git config --global user.email "actions@github.com"
    git add migufootball.m3u
    # 使用 || true 确保即使没有更改也不会导致流程失败
    git commit -m "🚀 自动更新咪咕足球m3u节目单-$(date +%Y%m%d)" || echo "No changes to commit, skipping..."
    git push
  env:
    GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
